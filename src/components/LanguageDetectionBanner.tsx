// Banner de deteção de idioma desativado.
// Português (PT-PT) é o idioma padrão forçado da aplicação.
// O utilizador pode alterar manualmente pelo seletor no header.
const LanguageDetectionBanner = () => null;

export default LanguageDetectionBanner;
